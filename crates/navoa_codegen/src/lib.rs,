use navoa_parser::Statement;

pub struct CodeGen {
    instructions: Vec<u8>,
}

impl CodeGen {
    pub fn new() -> Self {
        Self {
            instructions: Vec::new(),
        }
    }

    pub fn compile(&mut self, statements: &[Statement]) -> Result<Vec<u8>, String> {
        self.instructions.clear();

        for statement in statements {
            match statement {
                Statement::Print(_expr) => {
                    // Lógica para compilar instrução de impressão
                }
                Statement::Expr(_expr) => {
                    // Lógica para compilar expressão isolada
                }
                Statement::If { condition: _condition, then_branch: _then_branch, else_branch: _else_branch } => {
                    // Lógica para compilar estrutura condicional se/senão
                }
                Statement::While { condition: _condition, body: _body } => {
                    // Lógica para compilar ciclo while
                }
                _ => {
                    // Tratamento genérico para outras instruções do AST
                }
            }
        }

        Ok(self.instructions.clone())
    }
}
