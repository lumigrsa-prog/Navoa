#[test]
fn test_basic_math() {
    // Exemplo de teste para operações aritméticas básicas
    // let result = run_test_code("10 + 5 * 2");
    // assert_eq!(result, Ok(Some(Value::Int(20))));
}

#[test]
fn test_variable_declaration() {
    // Exemplo de teste para variáveis
    // let result = run_test_code("var x = 42; x");
    // assert_eq!(result, Ok(Some(Value::Int(42))));
}

#[test]
fn test_control_flow_if() {
    // Exemplo de teste para blocos Condicionais
    // let result = run_test_code("if (true) { 1 } else { 2 }");
    // assert_eq!(result, Ok(Some(Value::Int(1))));
}
