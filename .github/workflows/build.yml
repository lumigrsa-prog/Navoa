name: Build Navoa Multiplatform App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Configurar Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android,armv7-linux-androideabi,x86_64-linux-android

      - name: Configurar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Gerar pasta nativa Android (se não existir)
        run: |
          if [ ! -d "android" ]; then
            flutter create --platforms=android --org com.navoa .
          fi

      - name: Garantir dependências no pubspec.yaml
        run: flutter pub add flutter_rust_bridge:^2.13.0

      - name: Instalar cargo-binstall, flutter_rust_bridge_codegen e cargo-ndk
        run: |
          curl -L --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/cargo-bins/cargo-binstall/main/install-from-binstall-release.sh | bash
          ~/.cargo/bin/cargo-binstall flutter_rust_bridge_codegen@^2.0.0 -y
          ~/.cargo/bin/cargo-binstall cargo-ndk -y

      - name: Gerar Bindings do Rust para Flutter
        run: ~/.cargo/bin/flutter_rust_bridge_codegen generate --rust-input rust/src/api/mod.rs --dart-output lib/src/rust/

      - name: Compilar bibliotecas Rust (.so) para Android
        run: |
          mkdir -p android/app/src/main/jniLibs
          ANDROID_NDK_HOME=$ANDROID_HOME/ndk-bundle ~/.cargo/bin/cargo ndk -t arm64-v8a -t armeabi-v7a -t x86_64 -o android/app/src/main/jniLibs build --release --manifest-path rust/Cargo.toml || \
          ANDROID_NDK_HOME=$ANDROID_NDK_LATEST_HOME ~/.cargo/bin/cargo ndk -t arm64-v8a -t armeabi-v7a -t x86_64 -o android/app/src/main/jniLibs build --release --manifest-path rust/Cargo.toml

      - name: Instalar dependências do Flutter
        run: flutter pub get

      - name: Compilar APK Android
        run: flutter build apk --release

      - name: Guardar APK como Artefacto
        uses: actions/upload-artifact@v4
        with:
          name: Navoa-Android-APK
          path: build/app/outputs/flutter-apk/app-release.apk
